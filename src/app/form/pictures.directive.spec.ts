import { PicturesDirective } from './pictures.directive';

describe('PicturesDirective', () => {
  it('should create an instance', () => {
    const directive = new PicturesDirective();
    expect(directive).toBeTruthy();
  });
});
